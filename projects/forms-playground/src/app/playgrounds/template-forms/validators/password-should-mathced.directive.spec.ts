import { PasswordShouldMatchedDirective } from "./password-should-matched.directive";

describe('PasswordShouldMathcedDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordShouldMatchedDirective();
    expect(directive).toBeTruthy();
  });
});
